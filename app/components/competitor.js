import Component from '@glimmer/component';
import { STARSHIP, CHARACTER } from '../constants';

export default class CompetitorComponent extends Component {
  STARSHIP = STARSHIP;
  CHARACTER = CHARACTER;
}
